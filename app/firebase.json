{
  "hosting": [{
    "target": "staging",
    "public": "build",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]}
  ],
  "database": {
    "rules": "database.rules.json"
  }
}
