import InitialState from 'constants/InitialState';

const data = (state = InitialState.data, action) => {
  switch (action.type) {
    case 'ACTION':
      return state
  }
  return state
}

export default data;
